const nomes = ['Maria', 'João', 'Eduardo', 'Gabriel', 'Julia'];

//nomes.splice(indice, delte, elem1, elem2, elem3);

// const removidos = nomes.splice(-1, 1, 'Luiz');

const removidos = nomes.splice(nomes.length, 0, 'Luiz');
const removidos = nomes.splice(nomes.length, 0, 'Luiz');
console.log(nomes, removidos);
